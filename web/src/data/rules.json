{
  "_generated_by": "scripts/generate_rule_docs.py -- edit devrepro/rules/catalog.py",
  "count": 149,
  "manager_conflict_ecosystems": [
    "node",
    "python"
  ],
  "rules": [
    {
      "rule_id": "abi/glibc-below-common-wheel-tag",
      "prefix": "abi",
      "suffix": "glibc-below-common-wheel-tag",
      "title": "glibc is older than the wheels most projects publish",
      "means": "The installed glibc is below the floor required by the `manylinux` tag the ecosystem has largely moved to.",
      "matters": "Wheels carrying that tag are skipped silently and pip builds from source, which is how `pip install numpy` turns into a fifteen-minute compile that fails on a missing header. The finding names the tags this machine *can* install, because that is the part you can act on.",
      "fix": "Use a newer base image or distribution release. Pinning older releases of each package works and is a treadmill; the glibc floor only moves in one direction.",
      "composed": false
    },
    {
      "rule_id": "abi/glibc-ok",
      "prefix": "abi",
      "suffix": "glibc-ok",
      "title": "glibc accepts the wheels most projects publish",
      "means": "The installed glibc meets the floor for current `manylinux` tags.",
      "matters": "Recorded as a PASS because the absence of a finding and a verified match are different states, and a report that only lists problems cannot tell you which of the two it means.",
      "fix": "Nothing to do.",
      "composed": false
    },
    {
      "rule_id": "abi/interpreter-arch-mismatch",
      "prefix": "abi",
      "suffix": "interpreter-arch-mismatch",
      "title": "The interpreter and the machine disagree about the architecture",
      "means": "The process asking for packages reports one architecture while the host reports another.",
      "matters": "Package managers choose prebuilt artefacts by the *interpreter's* architecture, not the machine's, so everything installed through this interpreter is built for the wrong one. Installation succeeds every time; the cost appears as slow builds and as extensions that will not load elsewhere.",
      "fix": "Reinstall the runtime for this machine's architecture. A runtime carried across from an Intel Mac by a migration assistant is the usual cause, and it keeps working well enough to go unnoticed.",
      "composed": false
    },
    {
      "rule_id": "abi/musl-libc",
      "prefix": "abi",
      "suffix": "musl-libc",
      "title": "This machine uses musl, so manylinux wheels will not load",
      "means": "The C library is musl rather than glibc -- Alpine, and images derived from it, are the common case.",
      "matters": "A `manylinux` wheel is linked against glibc and cannot load here. pip does not report that: it skips the wheel and builds from source instead, which needs a compiler and development headers that a slim image deliberately does not carry. The error names a missing header file, and the actual cause -- the C library -- is never mentioned.",
      "fix": "Install the `musllinux` wheel where the project publishes one. Where it does not, add the build toolchain deliberately rather than discovering the need part-way through an install.",
      "composed": false
    },
    {
      "rule_id": "abi/runtime-arch-mismatch",
      "prefix": "abi",
      "suffix": "runtime-arch-mismatch",
      "title": "A runtime reports a different architecture from the machine",
      "means": "`node`, `go` or a similar runtime answers with an architecture the host does not share.",
      "matters": "Prebuilt native addons are selected by the runtime's own architecture. A mismatch means every one of them is the emulated build, and any addon compiled here will not load in a native process -- which is how a `node_modules` directory becomes non-portable between two machines that look identical.",
      "fix": "Reinstall the runtime for this architecture and rebuild its native dependencies. For node, that is `npm rebuild` after removing `node_modules`.",
      "composed": false
    },
    {
      "rule_id": "abi/translated-process",
      "prefix": "abi",
      "suffix": "translated-process",
      "title": "This process runs under Rosetta translation",
      "means": "The interpreter is an x86_64 build running on Apple silicon, with macOS translating every instruction.",
      "matters": "Nothing fails, which is the problem. Every package installed from here is the x86_64 build -- wheels, native addons, compiled extensions -- and they stay x86_64 for anything that loads them later. Builds take roughly ten times as long, and an extension compiled here will not load in a colleague's native arm64 process. The machine reports itself as arm64 throughout.",
      "fix": "Install an arm64 build of the runtime and re-create the virtualenv or `node_modules` from scratch. Reinstalling packages into the existing one keeps the x86_64 artefacts that are already there.",
      "composed": false
    },
    {
      "rule_id": "advisories/coverage",
      "prefix": "advisories",
      "suffix": "coverage",
      "title": "What the advisory set covers, and when it was reviewed",
      "means": "Names the advisory data in use, the date it was last reviewed, and the tools it has any data for at all.",
      "matters": "An offline advisory set that is silent about a tool has given no answer, and silence reads as a clean result. Reporting coverage explicitly is the only thing that keeps those two apart. The date matters for the same reason: an old bundle is not a clean machine.",
      "fix": "Nothing to fix. To use a different or newer set, run `devrepro advisories --db <bundle.json>`; an external bundle needs a signature beside it and `DEVREPRO_ADVISORY_KEY` set.",
      "composed": false
    },
    {
      "rule_id": "caches/compiler-cache-effective",
      "prefix": "caches",
      "suffix": "compiler-cache-effective",
      "title": "The compiler cache is earning its place",
      "means": "The reported hit rate is high enough that the cache is saving more work than it costs.",
      "matters": "Recorded as a PASS because the absence of a finding and a verified measurement are different states, and a report that only lists problems cannot tell you which of the two it means.",
      "fix": "Nothing to do.",
      "composed": false
    },
    {
      "rule_id": "caches/compiler-cache-full",
      "prefix": "caches",
      "suffix": "compiler-cache-full",
      "title": "The compiler cache is at its configured size limit",
      "means": "Reported size has reached the maximum the cache was given.",
      "matters": "A cache at its ceiling evicts what it is about to need again, which is usually the *cause* of a low hit rate rather than a separate problem. It is reported alongside the rate because the rate is the symptom and this is the thing to change.",
      "fix": "Raise `max_size` (ccache) or `SCCACHE_CACHE_SIZE`. Both default to values chosen when repositories were smaller.",
      "composed": false
    },
    {
      "rule_id": "caches/compiler-cache-ineffective",
      "prefix": "caches",
      "suffix": "compiler-cache-ineffective",
      "title": "The compiler cache is costing more than it saves",
      "means": "`ccache` or `sccache` reports a hit rate low enough that the cache is adding work rather than removing it.",
      "matters": "Every miss pays a lookup, a write and an eviction on top of the compile it did not avoid. The usual cause is a cache smaller than the working set, so it evicts what it is about to need again. The symptom people report is \"builds got slower after we turned caching on\", which nobody attributes to the cache, and the numbers that would explain it are published by the cache itself and read by nothing.",
      "fix": "Raise the size limit -- `ccache --max-size` or `SCCACHE_CACHE_SIZE`. If the cache is not full, look for something that varies on every run: an absolute path, a timestamp or a build id in the compiler command line defeats caching entirely.",
      "composed": false
    },
    {
      "rule_id": "caches/disk-critical",
      "prefix": "caches",
      "suffix": "disk-critical",
      "title": "Free disk space is nearly exhausted",
      "means": "So little space remains that ordinary operations will fail.",
      "matters": "A container build, a dependency install or even a git checkout will stop partway through, and the error will name whichever step happened to be running rather than the disk.",
      "fix": "Reclaim space before doing anything else. `docker system df` and the cache inventory in this report both name candidates; neither is pruned automatically, because which data is expendable is not something a diagnostic can know.",
      "composed": false
    },
    {
      "rule_id": "caches/disk-low",
      "prefix": "caches",
      "suffix": "disk-low",
      "title": "Free disk space is low",
      "means": "Less space remains than a cold dependency install or a container image pull typically needs.",
      "matters": "Enough for now, and not for the next `npm ci` or `docker pull`. The failure arrives mid-build as \"no space left on device\", from a step that has nothing to do with the cause.",
      "fix": "Build caches are usually the largest reclaimable thing on a developer machine. This scan lists the ones present rather than sizing them -- walking a Gradle cache costs more than the whole scan -- and each finding carries the command that measures it.",
      "composed": false
    },
    {
      "rule_id": "caches/relocated",
      "prefix": "caches",
      "suffix": "relocated",
      "title": "A build cache has been moved by an environment variable",
      "means": "One or more caches are not in their default location, because a variable such as `PIP_CACHE_DIR` or `GRADLE_USER_HOME` points elsewhere.",
      "matters": "Usually deliberate and reported for one reason: a cache redirected onto a network share, an external volume, or a directory a cleanup job empties overnight is a common cause of \"builds are slow on this machine only\", with nothing in the build output that would explain it.",
      "fix": "Nothing, if the redirection was intended. If it was not, unset the variable; the default location is on the fastest disk the machine has, which is normally the point.",
      "composed": false
    },
    {
      "rule_id": "containers/arch-emulated",
      "prefix": "containers",
      "suffix": "arch-emulated",
      "title": "The container engine is emulating another architecture",
      "means": "The daemon reports a different CPU architecture from the host, so every build and every container runs through qemu.",
      "matters": "It works, which is why nobody notices. It is also roughly ten times slower, and the usual cause is a base image with no manifest for the host architecture -- so one line in a Dockerfile silently turns a one-minute build into a twenty-minute one.",
      "fix": "Use a base image that publishes your architecture, or pass `--platform` explicitly so the emulation is a decision rather than a surprise. If the target really is the other architecture, nothing here is wrong.",
      "composed": false
    },
    {
      "rule_id": "containers/buildkit-unavailable",
      "prefix": "containers",
      "suffix": "buildkit-unavailable",
      "title": "docker buildx is not available",
      "means": "The daemon answers, but the buildx plugin is absent, so builds fall back to the legacy builder.",
      "matters": "Multi-platform builds, build secrets, cache mounts and `--mount=type=cache` all require BuildKit. A Dockerfile using any of them fails with a syntax error rather than a message about the builder, which sends people looking in the wrong file.",
      "fix": "Install the buildx plugin from your package manager, or use a Docker distribution that bundles it. Nothing needs changing if no Dockerfile here uses BuildKit features.",
      "composed": false
    },
    {
      "rule_id": "containers/cgroup-v1",
      "prefix": "containers",
      "suffix": "cgroup-v1",
      "title": "The container engine is using cgroup v1",
      "means": "Resource limits are enforced through the first-generation cgroup interface rather than the unified hierarchy.",
      "matters": "Memory accounting differs between the two, so a container that is OOM-killed on a v2 CI runner can pass locally on v1 and the other way round. Swap accounting is often absent entirely under v1, which makes `--memory` mean something different from what the docs say.",
      "fix": "Enable unified cgroups on the host, or switch it on in Docker Desktop's settings. On a distribution still defaulting to v1, `systemd.unified_cgroup_hierarchy=1` on the kernel command line does it.",
      "composed": false
    },
    {
      "rule_id": "containers/devcontainer-required",
      "prefix": "containers",
      "suffix": "devcontainer-required",
      "title": "Policy requires a devcontainer definition",
      "means": "The policy sets `require_devcontainer`, and no `.devcontainer/devcontainer.json` was found.",
      "matters": "The team decided one-command reproducible environments are the standard here, and this repository does not meet it.",
      "fix": "Run `devrepro generate devcontainer` for a reviewable draft, then adjust and commit it.",
      "composed": false
    },
    {
      "rule_id": "containers/disk-reclaimable",
      "prefix": "containers",
      "suffix": "disk-reclaimable",
      "title": "Container storage holds a large amount of reclaimable space",
      "means": "The daemon's own accounting says a substantial share of its images, volumes and build cache is unreferenced.",
      "matters": "Running out of space mid-build produces one of the least informative errors in the ecosystem -- `no space left on device`, from a step that has nothing to do with the cause -- and it usually arrives with tens of gigabytes of dangling layers sitting behind it. Reporting it before it becomes that error is the only useful moment.",
      "fix": "`docker system prune -a --volumes` reclaims it. This tool does not run it and will not offer to: pruning deletes data, and which data is expendable is not something a diagnostic can know.",
      "composed": false
    },
    {
      "rule_id": "containers/docker-daemon-error",
      "prefix": "containers",
      "suffix": "docker-daemon-error",
      "title": "Docker daemon returned an error this project does not classify",
      "means": "`docker info` failed with a message that matched none of the known patterns.",
      "matters": "Reported verbatim rather than guessed at. A specific wrong diagnosis would send you further from the cause than an honest unknown.",
      "fix": "Read the evidence excerpt in the finding; it carries the daemon's own message. If the shape is common, it is worth a classifier branch.",
      "composed": false
    },
    {
      "rule_id": "containers/docker-daemon-permission",
      "prefix": "containers",
      "suffix": "docker-daemon-permission",
      "title": "Docker daemon refused this user",
      "means": "The daemon is running, but this user is not permitted to talk to its socket.",
      "matters": "Every container command fails with a permission error that looks like the daemon is down. It is not: the daemon is fine and the user is not in the right group.",
      "fix": "On Linux, add your user to the `docker` group and start a new login session. Prefer that to running the CLI with sudo, which leaves root-owned files in your build outputs.",
      "composed": false
    },
    {
      "rule_id": "containers/docker-daemon-pipe-missing",
      "prefix": "containers",
      "suffix": "docker-daemon-pipe-missing",
      "title": "Docker Desktop's named pipe is absent",
      "means": "The CLI tried to reach the daemon over its Windows named pipe and the pipe does not exist.",
      "matters": "This is what a stopped Docker Desktop looks like from the CLI's side. The message names the pipe rather than the application, so it reads like a configuration fault when it is simply not running.",
      "fix": "Start Docker Desktop and wait for it to report running, then re-run `devrepro doctor`.",
      "composed": false
    },
    {
      "rule_id": "containers/docker-daemon-unreachable",
      "prefix": "containers",
      "suffix": "docker-daemon-unreachable",
      "title": "Docker CLI present, daemon not answering",
      "means": "The `docker` command exists, but `docker info` cannot reach a daemon.",
      "matters": "Every container-based build, test and devcontainer fails until the daemon is running. The CLI being installed makes this look like a working setup when it is not.",
      "fix": "Start Docker Desktop, or `systemctl start docker` on Linux. On Windows check that the WSL backend is healthy.",
      "composed": false
    },
    {
      "rule_id": "containers/docker-docker-wsl-backend-error",
      "prefix": "containers",
      "suffix": "docker-docker-wsl-backend-error",
      "title": "Docker's WSL backend is unhealthy",
      "means": "The daemon could not be reached and the error names WSL.",
      "matters": "Docker Desktop on Windows runs its engine inside WSL2. When that distribution is stopped, corrupt, or was updated underneath Docker, the CLI reports a connection failure that is really a WSL failure.",
      "fix": "Check `wsl --list --verbose` and `wsl --status`. Restarting WSL (`wsl --shutdown`, then start Docker Desktop) resolves most of these.",
      "composed": false
    },
    {
      "rule_id": "containers/docker-missing",
      "prefix": "containers",
      "suffix": "docker-missing",
      "title": "Docker CLI not installed",
      "means": "No `docker` executable resolves on PATH.",
      "matters": "Reported as INFO, not an error: plenty of projects never need it. It matters only when the project declares a container requirement.",
      "fix": "Install Docker only if this project needs it. `devrepro project` shows what is actually declared.",
      "composed": false
    },
    {
      "rule_id": "containers/healthy",
      "prefix": "containers",
      "suffix": "healthy",
      "title": "Container tooling is healthy",
      "means": "The container runtime responded normally.",
      "matters": "Reported so a working container stack is visible in the report rather than inferred from the absence of an error.",
      "fix": "No action needed.",
      "composed": false
    },
    {
      "rule_id": "containers/known-bad-version",
      "prefix": "containers",
      "suffix": "known-bad-version",
      "title": "Version explicitly forbidden by policy",
      "means": "The installed version matches a range listed under `[known_bad_versions]` in the project's `.devrepro.toml`.",
      "matters": "Someone recorded that this exact version breaks the project. That is more specific than a range check and usually earned the hard way.",
      "fix": "Move off the forbidden version. The policy file should say why; if it does not, add the reason for whoever hits it next.",
      "composed": true
    },
    {
      "rule_id": "containers/missing",
      "prefix": "containers",
      "suffix": "missing",
      "title": "Required tool not found",
      "means": "The project declares this tool as a requirement and it does not resolve on PATH.",
      "matters": "Any command that needs it fails immediately, usually with a confusing 'not recognized' or 'command not found' rather than a note about the declared requirement.",
      "fix": "Install it, or remove the requirement from the manifest if it is no longer real. Check `devrepro which <tool>` first -- the tool may be installed but shadowed or off PATH in this shell.",
      "composed": true
    },
    {
      "rule_id": "containers/multiple-runtimes",
      "prefix": "containers",
      "suffix": "multiple-runtimes",
      "title": "More than one container engine is installed",
      "means": "Two or more of Docker Desktop, Colima, Rancher Desktop, OrbStack, Podman, Lima and minikube resolve on PATH.",
      "matters": "This is not a fault -- plenty of people keep Colima beside Docker Desktop deliberately. It is worth knowing because it makes `docker context` load-bearing: a context pointing at a stopped VM produces exactly the error you would get with no daemon at all, and the obvious fix (start Docker Desktop) then changes nothing.",
      "fix": "`docker context ls` shows which engine `docker` currently talks to, and `docker context use <name>` switches it.",
      "composed": false
    },
    {
      "rule_id": "containers/state-unknown",
      "prefix": "containers",
      "suffix": "state-unknown",
      "title": "Container state could not be determined",
      "means": "The probe could not establish whether container tooling works.",
      "matters": "Reported as unknown rather than healthy. A diagnostic that guesses is worse than one that admits the gap.",
      "fix": "Run `docker info` by hand to see what it says.",
      "composed": false
    },
    {
      "rule_id": "containers/storage-driver-legacy",
      "prefix": "containers",
      "suffix": "storage-driver-legacy",
      "title": "The storage driver is deprecated, removed, or has no copy-on-write",
      "means": "The daemon is using a storage driver that current engines no longer recommend or no longer ship.",
      "matters": "`aufs` and `devicemapper` are removed in recent Docker releases, so an upgrade will stop the daemon starting. `vfs` is the one that surprises people: it is correct, it is what you get when nothing else is available, and it copies the entire filesystem for every layer.",
      "fix": "Move to overlay2. Changing the storage driver discards existing images and containers, so do it when you can afford to rebuild rather than in the middle of something.",
      "composed": false
    },
    {
      "rule_id": "containers/version-mismatch",
      "prefix": "containers",
      "suffix": "version-mismatch",
      "title": "Version outside the declared range",
      "means": "The tool is installed, but its version does not satisfy what the project's manifest requires.",
      "matters": "This is the most common cause of a build that works for one person and not another. Lockfiles, syntax features and native modules are all version-sensitive, and the failure usually surfaces far from the cause.",
      "fix": "Install a version inside the declared range, or widen the range if the declaration is stale. A version manager (mise, asdf, nvm, pyenv) makes this per-project rather than global.",
      "composed": true
    },
    {
      "rule_id": "containers/version-ok",
      "prefix": "containers",
      "suffix": "version-ok",
      "title": "Version satisfies the requirement",
      "means": "The installed version falls inside the range the project declares.",
      "matters": "Nothing to do. It is reported so a passing check is visible rather than inferred from silence.",
      "fix": "No action needed.",
      "composed": true
    },
    {
      "rule_id": "containers/version-unparseable",
      "prefix": "containers",
      "suffix": "version-unparseable",
      "title": "Version string could not be read",
      "means": "The tool responded, but its version output did not match any pattern this project knows how to parse.",
      "matters": "The requirement cannot be checked either way, so this is reported as unknown rather than passing. A silent pass here would be a lie.",
      "fix": "Run the tool's version command by hand and open an issue with the output, so the parser can learn the shape.",
      "composed": true
    },
    {
      "rule_id": "cpp/known-bad-version",
      "prefix": "cpp",
      "suffix": "known-bad-version",
      "title": "Version explicitly forbidden by policy",
      "means": "The installed version matches a range listed under `[known_bad_versions]` in the project's `.devrepro.toml`.",
      "matters": "Someone recorded that this exact version breaks the project. That is more specific than a range check and usually earned the hard way.",
      "fix": "Move off the forbidden version. The policy file should say why; if it does not, add the reason for whoever hits it next.",
      "composed": true
    },
    {
      "rule_id": "cpp/missing",
      "prefix": "cpp",
      "suffix": "missing",
      "title": "Required tool not found",
      "means": "The project declares this tool as a requirement and it does not resolve on PATH.",
      "matters": "Any command that needs it fails immediately, usually with a confusing 'not recognized' or 'command not found' rather than a note about the declared requirement.",
      "fix": "Install it, or remove the requirement from the manifest if it is no longer real. Check `devrepro which <tool>` first -- the tool may be installed but shadowed or off PATH in this shell.",
      "composed": true
    },
    {
      "rule_id": "cpp/no-compiler",
      "prefix": "cpp",
      "suffix": "no-compiler",
      "title": "No C/C++ compiler found",
      "means": "No MSVC, gcc or clang resolves on PATH.",
      "matters": "Native extensions cannot build. This surfaces late and confusingly: `pip install` or `npm install` fails deep inside a build log rather than saying a compiler is missing.",
      "fix": "Install build tools for your platform -- Visual Studio Build Tools, Xcode Command Line Tools, or your distribution's build-essential.",
      "composed": false
    },
    {
      "rule_id": "cpp/version-mismatch",
      "prefix": "cpp",
      "suffix": "version-mismatch",
      "title": "Version outside the declared range",
      "means": "The tool is installed, but its version does not satisfy what the project's manifest requires.",
      "matters": "This is the most common cause of a build that works for one person and not another. Lockfiles, syntax features and native modules are all version-sensitive, and the failure usually surfaces far from the cause.",
      "fix": "Install a version inside the declared range, or widen the range if the declaration is stale. A version manager (mise, asdf, nvm, pyenv) makes this per-project rather than global.",
      "composed": true
    },
    {
      "rule_id": "cpp/version-ok",
      "prefix": "cpp",
      "suffix": "version-ok",
      "title": "Version satisfies the requirement",
      "means": "The installed version falls inside the range the project declares.",
      "matters": "Nothing to do. It is reported so a passing check is visible rather than inferred from silence.",
      "fix": "No action needed.",
      "composed": true
    },
    {
      "rule_id": "cpp/version-unparseable",
      "prefix": "cpp",
      "suffix": "version-unparseable",
      "title": "Version string could not be read",
      "means": "The tool responded, but its version output did not match any pattern this project knows how to parse.",
      "matters": "The requirement cannot be checked either way, so this is reported as unknown rather than passing. A silent pass here would be a lie.",
      "fix": "Run the tool's version command by hand and open an issue with the output, so the parser can learn the shape.",
      "composed": true
    },
    {
      "rule_id": "docker/multiple-installations",
      "prefix": "docker",
      "suffix": "multiple-installations",
      "title": "Several copies of the same tool on PATH",
      "means": "More than one executable with this name resolves on PATH. The first wins, and the others are reachable only by full path.",
      "matters": "The one that wins depends on PATH order, which differs between your shell, your editor's terminal, and CI. That is how the same command produces different versions in different windows on one machine.",
      "fix": "Run `devrepro which <tool>` to see every candidate and which one wins. Keep the installation you intend to use and remove or de-prefer the rest; `devrepro plan` proposes the PATH edit.",
      "composed": true
    },
    {
      "rule_id": "dotnet/global-json-satisfied",
      "prefix": "dotnet",
      "suffix": "global-json-satisfied",
      "title": "An installed .NET SDK satisfies global.json",
      "means": "One of the SDKs on this machine matches the pin under its `rollForward` policy.",
      "matters": "Recorded as a PASS because the absence of a finding and a verified match are different states, and a report that only lists problems cannot tell you which of the two it means.",
      "fix": "Nothing to do.",
      "composed": false
    },
    {
      "rule_id": "dotnet/global-json-sdk-missing",
      "prefix": "dotnet",
      "suffix": "global-json-sdk-missing",
      "title": "global.json pins a .NET SDK that is not installed",
      "means": "The repository's `global.json` requests an SDK version, and no installed SDK satisfies it under the file's `rollForward` policy.",
      "matters": "`dotnet build` fails with \"A compatible .NET SDK was not found\", while `dotnet --version` prints one of the SDKs that *is* installed and every other check passes -- which is why this reads as a working installation right up until the build. With `rollForward` unset the pin is exact to the feature band, so 8.0.100 and 8.0.404 are not interchangeable.",
      "fix": "Install the pinned SDK, or set `rollForward` in `global.json` if a newer one is genuinely acceptable. Deleting the pin works and gives up the reason it was added.",
      "composed": false
    },
    {
      "rule_id": "dotnet/known-bad-version",
      "prefix": "dotnet",
      "suffix": "known-bad-version",
      "title": "Version explicitly forbidden by policy",
      "means": "The installed version matches a range listed under `[known_bad_versions]` in the project's `.devrepro.toml`.",
      "matters": "Someone recorded that this exact version breaks the project. That is more specific than a range check and usually earned the hard way.",
      "fix": "Move off the forbidden version. The policy file should say why; if it does not, add the reason for whoever hits it next.",
      "composed": true
    },
    {
      "rule_id": "dotnet/missing",
      "prefix": "dotnet",
      "suffix": "missing",
      "title": "Required tool not found",
      "means": "The project declares this tool as a requirement and it does not resolve on PATH.",
      "matters": "Any command that needs it fails immediately, usually with a confusing 'not recognized' or 'command not found' rather than a note about the declared requirement.",
      "fix": "Install it, or remove the requirement from the manifest if it is no longer real. Check `devrepro which <tool>` first -- the tool may be installed but shadowed or off PATH in this shell.",
      "composed": true
    },
    {
      "rule_id": "dotnet/sdk-list-unavailable",
      "prefix": "dotnet",
      "suffix": "sdk-list-unavailable",
      "title": "The installed .NET SDKs could not be listed",
      "means": "`global.json` pins an SDK, but `dotnet --list-sdks` did not answer.",
      "matters": "Whether the pin is satisfiable here is genuinely unknown, and reporting it as a blocker would be a guess. This is UNKNOWN rather than BLOCKED for that reason.",
      "fix": "Install the .NET SDK if this project is built on this machine. If it is not, nothing here needs changing.",
      "composed": false
    },
    {
      "rule_id": "dotnet/version-mismatch",
      "prefix": "dotnet",
      "suffix": "version-mismatch",
      "title": "Version outside the declared range",
      "means": "The tool is installed, but its version does not satisfy what the project's manifest requires.",
      "matters": "This is the most common cause of a build that works for one person and not another. Lockfiles, syntax features and native modules are all version-sensitive, and the failure usually surfaces far from the cause.",
      "fix": "Install a version inside the declared range, or widen the range if the declaration is stale. A version manager (mise, asdf, nvm, pyenv) makes this per-project rather than global.",
      "composed": true
    },
    {
      "rule_id": "dotnet/version-ok",
      "prefix": "dotnet",
      "suffix": "version-ok",
      "title": "Version satisfies the requirement",
      "means": "The installed version falls inside the range the project declares.",
      "matters": "Nothing to do. It is reported so a passing check is visible rather than inferred from silence.",
      "fix": "No action needed.",
      "composed": true
    },
    {
      "rule_id": "dotnet/version-unparseable",
      "prefix": "dotnet",
      "suffix": "version-unparseable",
      "title": "Version string could not be read",
      "means": "The tool responded, but its version output did not match any pattern this project knows how to parse.",
      "matters": "The requirement cannot be checked either way, so this is reported as unknown rather than passing. A silent pass here would be a lie.",
      "fix": "Run the tool's version command by hand and open an issue with the output, so the parser can learn the shape.",
      "composed": true
    },
    {
      "rule_id": "env/credential-names-present",
      "prefix": "env",
      "suffix": "credential-names-present",
      "title": "Credential-shaped variable names in this session",
      "means": "Variables whose NAMES look like credentials exist in the environment. Their values are never read.",
      "matters": "Informational, and deliberately name-only: it tells you secrets are present in this shell without ever handling them.",
      "fix": "No action needed. It is context for anyone reading the report.",
      "composed": false
    },
    {
      "rule_id": "env/required-names-present",
      "prefix": "env",
      "suffix": "required-names-present",
      "title": "Required environment variable names",
      "means": "Whether the variable NAMES the policy requires are set. Values are never read, printed or stored.",
      "matters": "Missing configuration surfaces as a runtime failure far from the cause. Checking names catches it before the process starts.",
      "fix": "Set the missing variables in your shell profile or .env loader.",
      "composed": false
    },
    {
      "rule_id": "git/credential-helper-missing",
      "prefix": "git",
      "suffix": "credential-helper-missing",
      "title": "A credential helper is configured but not installed",
      "means": "`credential.helper` names a helper that resolves to no program in git's exec directory or on PATH.",
      "matters": "Every authenticated fetch, clone and push falls back to prompting. Interactively that is a confusing password request for a repository you thought was public; in CI it is a hang followed by a timeout, and nothing in the output names the helper. The setting usually survives a machine migration or an OS change that the helper did not.",
      "fix": "Install the helper, or clear the setting with `git config --global --unset credential.helper`. devrepro reads the helper's name and never runs it -- several of them block on stdin.",
      "composed": false
    },
    {
      "rule_id": "git/credential-store-plaintext",
      "prefix": "git",
      "suffix": "credential-store-plaintext",
      "title": "The `store` credential helper keeps tokens in plain text",
      "means": "`credential.helper=store` writes credentials to `~/.git-credentials` unencrypted.",
      "matters": "Reported as information rather than a problem: on a single-user machine it is a considered choice, and it is the only helper that works everywhere. It is worth knowing because the file is readable by anything running as you -- including any package postinstall script -- and because backups and sync tools copy it without asking.",
      "fix": "An OS keychain helper (`osxkeychain`, `wincred`, `libsecret`) or Git Credential Manager stores the same tokens encrypted. devrepro reports the setting and never reads the file.",
      "composed": false
    },
    {
      "rule_id": "git/known-advisory",
      "prefix": "git",
      "suffix": "known-advisory",
      "title": "The installed tool version is covered by a published advisory",
      "means": "The version of this build tool matches an entry in the offline advisory set that ships with devrepro, or in the bundle you supplied with `--db`.",
      "matters": "Dependency scanners never look at the compiler, the interpreter or git, because none of them appear in a lockfile. A toolchain can therefore sit years out of date behind a perfectly clean audit.",
      "fix": "Upgrade the tool to the fix on its own release branch -- backported fixes mean the highest version number is not always the answer. This comparison is offline and does not know whether your distribution backported the fix into the version string you have, which many do; check your distribution's changelog before treating it as urgent.",
      "composed": false
    },
    {
      "rule_id": "git/lfs-not-initialised",
      "prefix": "git",
      "suffix": "lfs-not-initialised",
      "title": "Git LFS is installed but its filters are not configured",
      "means": "`git lfs` exists, but `filter.lfs.smudge` is unset for this user and repository, so the clean/smudge filters never run.",
      "matters": "Same symptom as not having LFS at all -- pointer files in the working tree, a clean `git status` -- and a different fix, which is why the two are separate findings rather than one. Being told to install something already installed is how a person concludes the tool is wrong and stops reading it.",
      "fix": "`git lfs install` configures the filters; `git lfs pull` fetches what the current checkout missed.",
      "composed": false
    },
    {
      "rule_id": "git/lfs-ready",
      "prefix": "git",
      "suffix": "lfs-ready",
      "title": "Git LFS is installed and configured for this checkout",
      "means": "The repository declares LFS-tracked paths and the filters are in place.",
      "matters": "Recorded as a PASS because the absence of a finding and a verified match are different states, and a report that only lists problems cannot tell you which of the two it means.",
      "fix": "Nothing to do.",
      "composed": false
    },
    {
      "rule_id": "git/lfs-required-not-installed",
      "prefix": "git",
      "suffix": "lfs-required-not-installed",
      "title": "This repository needs Git LFS and it is not installed",
      "means": "A `.gitattributes` here routes files through `filter=lfs`, and `git lfs` does not resolve on this machine.",
      "matters": "Git does not fail. It writes the pointer files -- a few lines of text where a binary should be -- reports the working tree as clean, and leaves the failure for whatever opens those files later. The error you get says the image is corrupt or the archive ended unexpectedly, and names a file rather than a missing tool.",
      "fix": "Install git-lfs, run `git lfs install`, then `git lfs pull` to replace the pointers already in your working tree. Cloning again without installing LFS first produces the same pointers.",
      "composed": false
    },
    {
      "rule_id": "git/multiple-installations",
      "prefix": "git",
      "suffix": "multiple-installations",
      "title": "Several copies of the same tool on PATH",
      "means": "More than one executable with this name resolves on PATH. The first wins, and the others are reachable only by full path.",
      "matters": "The one that wins depends on PATH order, which differs between your shell, your editor's terminal, and CI. That is how the same command produces different versions in different windows on one machine.",
      "fix": "Run `devrepro which <tool>` to see every candidate and which one wins. Keep the installation you intend to use and remove or de-prefer the rest; `devrepro plan` proposes the PATH edit.",
      "composed": true
    },
    {
      "rule_id": "git/partial-clone",
      "prefix": "git",
      "suffix": "partial-clone",
      "title": "This is a partial clone; some objects are fetched on demand",
      "means": "A filter such as `blob:none` or `tree:0` is configured on the remote, so objects arrive lazily rather than at clone time.",
      "matters": "It is a deliberate and usually good trade. It matters here because an operation needing a missing blob reaches the network, and on an air-gapped or simply offline machine that failure reads as repository corruption rather than as a fetch that could not happen.",
      "fix": "Nothing, unless you work offline: `git fetch --refetch` with no filter materialises what is missing.",
      "composed": false
    },
    {
      "rule_id": "git/shallow-clone",
      "prefix": "git",
      "suffix": "shallow-clone",
      "title": "This is a shallow clone",
      "means": "History before the graft point is absent, usually from a `--depth` clone or a CI checkout that defaults to depth 1.",
      "matters": "`git describe` produces the wrong version or fails, `git blame` stops at the graft, and any diff against a base ref -- including the one `devrepro guard --scope changed` uses -- cannot resolve the base. None of those errors mention shallowness.",
      "fix": "`git fetch --unshallow`. In GitHub Actions, `fetch-depth: 0` on the checkout step; most CI systems have an equivalent.",
      "composed": false
    },
    {
      "rule_id": "git/sparse-checkout-active",
      "prefix": "git",
      "suffix": "sparse-checkout-active",
      "title": "Sparse checkout is on, so parts of the tree are deliberately absent",
      "means": "`core.sparseCheckout` is enabled and a pattern list decides which paths are materialised.",
      "matters": "This is normally deliberate and is reported rather than warned about. It earns a place because a build failing on a path that exists in the repository and not on disk has no other visible explanation -- `git status` is clean either way, and the file is present in every listing on the web.",
      "fix": "`git sparse-checkout list` shows what is included and `git sparse-checkout disable` restores the full tree. Nothing here needs changing if the narrowing was intended.",
      "composed": false
    },
    {
      "rule_id": "git/submodules-uninitialised",
      "prefix": "git",
      "suffix": "submodules-uninitialised",
      "title": "Declared submodules have not been checked out",
      "means": "`.gitmodules` names submodules whose directories are empty.",
      "matters": "An uninitialised submodule is an empty directory, not an error, so a build fails on a missing header or a missing module rather than on a missing submodule. `git status` says nothing, because as far as the outer repository is concerned nothing has changed.",
      "fix": "`git submodule update --init --recursive`. Adding `--recurse-submodules` to your clone avoids the state entirely.",
      "composed": false
    },
    {
      "rule_id": "go/known-bad-version",
      "prefix": "go",
      "suffix": "known-bad-version",
      "title": "Version explicitly forbidden by policy",
      "means": "The installed version matches a range listed under `[known_bad_versions]` in the project's `.devrepro.toml`.",
      "matters": "Someone recorded that this exact version breaks the project. That is more specific than a range check and usually earned the hard way.",
      "fix": "Move off the forbidden version. The policy file should say why; if it does not, add the reason for whoever hits it next.",
      "composed": true
    },
    {
      "rule_id": "go/missing",
      "prefix": "go",
      "suffix": "missing",
      "title": "Required tool not found",
      "means": "The project declares this tool as a requirement and it does not resolve on PATH.",
      "matters": "Any command that needs it fails immediately, usually with a confusing 'not recognized' or 'command not found' rather than a note about the declared requirement.",
      "fix": "Install it, or remove the requirement from the manifest if it is no longer real. Check `devrepro which <tool>` first -- the tool may be installed but shadowed or off PATH in this shell.",
      "composed": true
    },
    {
      "rule_id": "go/multiple-installations",
      "prefix": "go",
      "suffix": "multiple-installations",
      "title": "Several copies of the same tool on PATH",
      "means": "More than one executable with this name resolves on PATH. The first wins, and the others are reachable only by full path.",
      "matters": "The one that wins depends on PATH order, which differs between your shell, your editor's terminal, and CI. That is how the same command produces different versions in different windows on one machine.",
      "fix": "Run `devrepro which <tool>` to see every candidate and which one wins. Keep the installation you intend to use and remove or de-prefer the rest; `devrepro plan` proposes the PATH edit.",
      "composed": true
    },
    {
      "rule_id": "go/version-mismatch",
      "prefix": "go",
      "suffix": "version-mismatch",
      "title": "Version outside the declared range",
      "means": "The tool is installed, but its version does not satisfy what the project's manifest requires.",
      "matters": "This is the most common cause of a build that works for one person and not another. Lockfiles, syntax features and native modules are all version-sensitive, and the failure usually surfaces far from the cause.",
      "fix": "Install a version inside the declared range, or widen the range if the declaration is stale. A version manager (mise, asdf, nvm, pyenv) makes this per-project rather than global.",
      "composed": true
    },
    {
      "rule_id": "go/version-ok",
      "prefix": "go",
      "suffix": "version-ok",
      "title": "Version satisfies the requirement",
      "means": "The installed version falls inside the range the project declares.",
      "matters": "Nothing to do. It is reported so a passing check is visible rather than inferred from silence.",
      "fix": "No action needed.",
      "composed": true
    },
    {
      "rule_id": "go/version-unparseable",
      "prefix": "go",
      "suffix": "version-unparseable",
      "title": "Version string could not be read",
      "means": "The tool responded, but its version output did not match any pattern this project knows how to parse.",
      "matters": "The requirement cannot be checked either way, so this is reported as unknown rather than passing. A silent pass here would be a lie.",
      "fix": "Run the tool's version command by hand and open an issue with the output, so the parser can learn the shape.",
      "composed": true
    },
    {
      "rule_id": "gpu/cuda-compatible",
      "prefix": "gpu",
      "suffix": "cuda-compatible",
      "title": "The CUDA toolkit and the driver agree",
      "means": "The installed toolkit falls inside the major version the driver supports.",
      "matters": "Nothing to do. Reported so that a working CUDA setup is visible rather than inferred from the absence of a complaint.",
      "fix": "No action needed.",
      "composed": false
    },
    {
      "rule_id": "gpu/cuda-driver-old",
      "prefix": "gpu",
      "suffix": "cuda-driver-old",
      "title": "Driver too old for the installed CUDA toolkit",
      "means": "The NVIDIA driver version does not support the CUDA toolkit present.",
      "matters": "This is the specific incompatible pair, rather than the 'CUDA not found' that frameworks usually report. Training and inference either fail at load or silently fall back to CPU.",
      "fix": "Update the driver to one that supports the toolkit, or install a toolkit matching the driver.",
      "composed": false
    },
    {
      "rule_id": "gpu/cuda-driver-too-old",
      "prefix": "gpu",
      "suffix": "cuda-driver-too-old",
      "title": "The installed CUDA toolkit needs a newer driver than this one",
      "means": "The CUDA toolkit's major version is ahead of the highest CUDA runtime the installed NVIDIA driver can run, as the driver itself reports it.",
      "matters": "Anything built with nvcc fails at launch with 'CUDA driver version is insufficient for CUDA runtime version', which names neither version. People reinstall the toolkit, or the driver, more or less at random.",
      "fix": "Update the NVIDIA driver, or install a toolkit inside the major version the driver supports. Note that CUDA 11+ guarantees minor-version compatibility, so 12.4 on a driver reporting 12.2 is fine and is not what this reports. To see what a framework build expects: `python -c \"import torch; print(torch.version.cuda)\"`.",
      "composed": false
    },
    {
      "rule_id": "gpu/cuda-toolkit-absent",
      "prefix": "gpu",
      "suffix": "cuda-toolkit-absent",
      "title": "A driver is present and nvcc is not",
      "means": "`nvidia-smi` answered but no CUDA toolkit is on PATH, so the compiler half of the stack is unknown.",
      "matters": "Reported as information, not as a fault. A framework wheel ships its own CUDA runtime and never calls nvcc; the toolkit matters only if you compile CUDA code yourself.",
      "fix": "Install the CUDA toolkit only if you build CUDA sources. Otherwise nothing is missing.",
      "composed": false
    },
    {
      "rule_id": "gpu/cuda-toolkit-missing",
      "prefix": "gpu",
      "suffix": "cuda-toolkit-missing",
      "title": "NVIDIA GPU present, CUDA toolkit absent",
      "means": "A driver was detected but no CUDA toolkit was found.",
      "matters": "Frameworks fall back to CPU, usually without saying so clearly. The symptom is a job that runs but takes a hundred times longer.",
      "fix": "Install a CUDA toolkit compatible with the driver, or use a framework build that bundles its own runtime.",
      "composed": false
    },
    {
      "rule_id": "gpu/mixed-architectures",
      "prefix": "gpu",
      "suffix": "mixed-architectures",
      "title": "The GPUs in this machine have different compute capabilities",
      "means": "Two or more devices report different `sm_XX` architectures.",
      "matters": "A build targeting one architecture produces kernels the other card cannot execute, and the failure -- 'no kernel image is available for execution on the device' -- names neither the device nor the architecture. It also arrives at runtime, on whichever device the scheduler happened to pick, so it looks intermittent.",
      "fix": "Build for every architecture present (TORCH_CUDA_ARCH_LIST or CMAKE_CUDA_ARCHITECTURES covering them all), or pin the job to one device with CUDA_VISIBLE_DEVICES.",
      "composed": false
    },
    {
      "rule_id": "gpu/no-accelerator",
      "prefix": "gpu",
      "suffix": "no-accelerator",
      "title": "No GPU accelerator detected",
      "means": "No NVIDIA, ROCm, oneAPI, Metal or DirectML stack was found.",
      "matters": "Informational. It matters only if the project expects acceleration.",
      "fix": "No action needed on a CPU-only machine.",
      "composed": false
    },
    {
      "rule_id": "gpu/stack-detected",
      "prefix": "gpu",
      "suffix": "stack-detected",
      "title": "GPU/AI stack detected",
      "means": "An accelerator stack was found and its versions recorded.",
      "matters": "Recorded so a snapshot diff can show when the stack changes -- one of the most common causes of a model that trains on one machine only.",
      "fix": "No action needed.",
      "composed": false
    },
    {
      "rule_id": "gpu/state-unknown",
      "prefix": "gpu",
      "suffix": "state-unknown",
      "title": "GPU state could not be determined",
      "means": "The probe could not establish what accelerator stack is present.",
      "matters": "Reported as unknown rather than absent.",
      "fix": "Run `nvidia-smi` or the equivalent for your vendor by hand.",
      "composed": false
    },
    {
      "rule_id": "host/antivirus-not-defender",
      "prefix": "host",
      "suffix": "antivirus-not-defender",
      "title": "Defender's real-time protection is off, so its exclusions mean nothing",
      "means": "Defender answered and reported real-time protection disabled. On a machine that is not unprotected this usually means another antivirus product registered itself and Defender stood down.",
      "matters": "That other product's exclusion list is not visible from here, so this check cannot speak for whatever is actually scanning your build directories -- and scanning is a common cause of a Windows build being several times slower than the same build elsewhere.",
      "fix": "Nothing to fix here. If Windows builds are slow, check your antivirus product's own exclusion list for the project's dependency and output directories.",
      "composed": false
    },
    {
      "rule_id": "host/antivirus-scans-build-dirs",
      "prefix": "host",
      "suffix": "antivirus-scans-build-dirs",
      "title": "Real-time scanning inspects this project's build directories",
      "means": "Windows Defender's real-time protection is on and its exclusion list does not cover the dependency and output directories this project actually has.",
      "matters": "Every file a build opens is scanned synchronously, on files written seconds earlier by a tool the machine already trusts. This is routinely the largest single factor in a Windows build taking several times longer than the same build elsewhere, and it is invisible: nothing fails and nothing logs.",
      "fix": "Excluding them is a trade, not a fix: it is a genuine reduction in protection on a tree whose install scripts execute code you did not write. Whether it is worth it depends on the machine and on your organisation's rules. DevRepro changes nothing -- the finding carries the exact command if you decide it is.",
      "composed": false
    },
    {
      "rule_id": "host/antivirus-unknown",
      "prefix": "host",
      "suffix": "antivirus-unknown",
      "title": "Defender's configuration could not be read from this shell",
      "means": "`Get-MpComputerStatus` or `Get-MpPreference` did not answer. This is almost always a permissions result rather than an absent Defender.",
      "matters": "Reported rather than assumed, because from a non-elevated shell 'could not read the exclusions' and 'there are no exclusions' look identical and lead to opposite conclusions.",
      "fix": "Run `devrepro doctor` from an elevated shell to see which build directories real-time scanning inspects.",
      "composed": false
    },
    {
      "rule_id": "host/clock-unsynchronised",
      "prefix": "host",
      "suffix": "clock-unsynchronised",
      "title": "Nothing is correcting this machine's clock",
      "means": "No time-synchronisation service is running, or the configured source is the machine's own hardware clock -- which is synchronising with itself and correcting nothing.",
      "matters": "The clock has not drifted yet; it will. When it does, the first symptom is usually a TLS error blaming a certificate that is fine ('not yet valid', 'expired'), and somebody spends an afternoon on the certificate. It also breaks Kerberos outright and makes build tools rebuild -- or refuse to rebuild -- for reasons no log explains. This is the cause; clock skew is the symptom, detected separately and later.",
      "fix": "Windows: `w32tm /config /syncfromflags:domhier /update` on a domain machine, or point it at time.windows.com and start the Windows Time service. Linux: enable systemd-timesyncd or chrony.",
      "composed": false
    },
    {
      "rule_id": "host/slow-filesystem",
      "prefix": "host",
      "suffix": "slow-filesystem",
      "title": "The project is on a filesystem where every file operation is slow",
      "means": "The source tree sits on a WSL/Windows crossing, a network share or another mount where each file operation costs a millisecond instead of a microsecond.",
      "matters": "A dependency install performs hundreds of thousands of file operations. Three orders of magnitude on each is the difference between twenty seconds and twenty minutes -- and nothing is failing, so no profiler anybody runs will point at it. It reads as a slow build tool, which is where people go looking.",
      "fix": "Move the tree onto local storage. Inside WSL that means somewhere under ~, opened from the editor's WSL integration rather than through /mnt/c. On a network share, at minimum keep the dependency directory local -- most package managers accept a store or cache path outside the project.",
      "composed": false
    },
    {
      "rule_id": "hygiene/filesystem-case",
      "prefix": "hygiene",
      "suffix": "filesystem-case",
      "title": "Filesystem case sensitivity",
      "means": "Whether this filesystem distinguishes `Config.py` from `config.py`.",
      "matters": "Linux is case-sensitive; Windows and macOS are not, by default. A repository authored on Linux that contains two files differing only in case loses one of them on checkout elsewhere, with no error at any point -- git reports success and the working tree is wrong.",
      "fix": "Nothing to change about the filesystem. Avoid committing paths that differ only in case; `git config core.ignorecase` affects how git compares them but not what the filesystem stores.",
      "composed": false
    },
    {
      "rule_id": "hygiene/non-utf8-locale",
      "prefix": "hygiene",
      "suffix": "non-utf8-locale",
      "title": "Text encoding is not UTF-8",
      "means": "The preferred encoding a subprocess inherits is a legacy codepage rather than UTF-8.",
      "matters": "Any tool that writes a non-ASCII character to this console can die with UnicodeEncodeError, and filenames with accents may not round-trip. This project hit exactly that: a single arrow in a remediation hint ended `devrepro check` on a cp1252 console.",
      "fix": "Set `PYTHONUTF8=1` for Python tools, or enable the OS-wide UTF-8 option (Windows: Region settings, 'Beta: Use Unicode UTF-8'). Note that the OS-wide switch affects every application, so it is worth changing deliberately rather than casually.",
      "composed": false
    },
    {
      "rule_id": "hygiene/reserved-filename",
      "prefix": "hygiene",
      "suffix": "reserved-filename",
      "title": "A path uses a name Windows reserves",
      "means": "The repository contains a path whose name is a DOS device name -- `con`, `prn`, `aux`, `nul`, `com1`-`com9` or `lpt1`-`lpt9` -- with or without an extension, so `aux.js` counts.",
      "matters": "Windows cannot create the file at all. The clone fails with an error that names neither the file nor the reason, and the repository is simply unusable there.",
      "fix": "Rename the path. This is reported on every platform on purpose: the person who can still fix it cheaply is the one about to commit it, not the one who cannot clone it.",
      "composed": false
    },
    {
      "rule_id": "hygiene/symlinks-unavailable",
      "prefix": "hygiene",
      "suffix": "symlinks-unavailable",
      "title": "This process cannot create symlinks",
      "means": "On Windows, symlink creation needs Developer Mode or SeCreateSymbolicLinkPrivilege, and neither is available here.",
      "matters": "Git does not fail when it cannot create a symlink. It writes an ordinary file containing the link target instead, so the working tree differs from the commit while `git status` reports everything clean.",
      "fix": "Enable Developer Mode in Windows Settings, or work inside WSL where symlinks behave normally.",
      "composed": false
    },
    {
      "rule_id": "java/home-path-mismatch",
      "prefix": "java",
      "suffix": "home-path-mismatch",
      "title": "JAVA_HOME and the java on PATH are different JDKs",
      "means": "The JDK that `JAVA_HOME` points at reports a different version from the `java` the shell resolves.",
      "matters": "Maven and Gradle use `JAVA_HOME`; a shell script that calls `java` directly uses PATH. When they disagree, the build compiles against one JDK and runs on another, and the failure is an `UnsupportedClassVersionError` that names neither of them.",
      "fix": "Point `JAVA_HOME` at the JDK you intend to use and put its `bin` first on PATH. A toolchain block in the build file overrides both, so check that too if one is declared.",
      "composed": false
    },
    {
      "rule_id": "java/known-bad-version",
      "prefix": "java",
      "suffix": "known-bad-version",
      "title": "Version explicitly forbidden by policy",
      "means": "The installed version matches a range listed under `[known_bad_versions]` in the project's `.devrepro.toml`.",
      "matters": "Someone recorded that this exact version breaks the project. That is more specific than a range check and usually earned the hard way.",
      "fix": "Move off the forbidden version. The policy file should say why; if it does not, add the reason for whoever hits it next.",
      "composed": true
    },
    {
      "rule_id": "java/missing",
      "prefix": "java",
      "suffix": "missing",
      "title": "Required tool not found",
      "means": "The project declares this tool as a requirement and it does not resolve on PATH.",
      "matters": "Any command that needs it fails immediately, usually with a confusing 'not recognized' or 'command not found' rather than a note about the declared requirement.",
      "fix": "Install it, or remove the requirement from the manifest if it is no longer real. Check `devrepro which <tool>` first -- the tool may be installed but shadowed or off PATH in this shell.",
      "composed": true
    },
    {
      "rule_id": "java/multiple-installations",
      "prefix": "java",
      "suffix": "multiple-installations",
      "title": "Several copies of the same tool on PATH",
      "means": "More than one executable with this name resolves on PATH. The first wins, and the others are reachable only by full path.",
      "matters": "The one that wins depends on PATH order, which differs between your shell, your editor's terminal, and CI. That is how the same command produces different versions in different windows on one machine.",
      "fix": "Run `devrepro which <tool>` to see every candidate and which one wins. Keep the installation you intend to use and remove or de-prefer the rest; `devrepro plan` proposes the PATH edit.",
      "composed": true
    },
    {
      "rule_id": "java/multiple-jdks",
      "prefix": "java",
      "suffix": "multiple-jdks",
      "title": "Several JDKs are installed",
      "means": "More than one JDK was found in the usual installation directories.",
      "matters": "Not a fault -- most Java developers need several. It is worth knowing because which one a build uses depends on `JAVA_HOME`, on PATH, and on whichever toolchain block the build file declares: three settings that are frequently out of step, and none of which announces itself.",
      "fix": "Nothing, unless a build behaves unexpectedly. `java -version` and `echo $JAVA_HOME` together tell you which two of the three agree.",
      "composed": false
    },
    {
      "rule_id": "java/shim-bypassed",
      "prefix": "java",
      "suffix": "shim-bypassed",
      "title": "A version manager is installed but not being used",
      "means": "The manager's shim directory is on PATH, but another installation of this tool resolves before it.",
      "matters": "The manager still reports the version it intends -- `pyenv version` says 3.12 -- while every command gets something else, because resolution is decided by PATH order and nothing else. Neither tool is wrong about what it was asked, so nothing reports a problem, and the project's pinned version is quietly not in effect.",
      "fix": "Move the manager's shim directory earlier in PATH than the installation that currently wins. `devrepro which <tool>` lists every candidate in precedence order. Note that an IDE's integrated terminal often has a different PATH from a login shell, so check the one you actually build in.",
      "composed": true
    },
    {
      "rule_id": "java/version-mismatch",
      "prefix": "java",
      "suffix": "version-mismatch",
      "title": "Version outside the declared range",
      "means": "The tool is installed, but its version does not satisfy what the project's manifest requires.",
      "matters": "This is the most common cause of a build that works for one person and not another. Lockfiles, syntax features and native modules are all version-sensitive, and the failure usually surfaces far from the cause.",
      "fix": "Install a version inside the declared range, or widen the range if the declaration is stale. A version manager (mise, asdf, nvm, pyenv) makes this per-project rather than global.",
      "composed": true
    },
    {
      "rule_id": "java/version-ok",
      "prefix": "java",
      "suffix": "version-ok",
      "title": "Version satisfies the requirement",
      "means": "The installed version falls inside the range the project declares.",
      "matters": "Nothing to do. It is reported so a passing check is visible rather than inferred from silence.",
      "fix": "No action needed.",
      "composed": true
    },
    {
      "rule_id": "java/version-unparseable",
      "prefix": "java",
      "suffix": "version-unparseable",
      "title": "Version string could not be read",
      "means": "The tool responded, but its version output did not match any pattern this project knows how to parse.",
      "matters": "The requirement cannot be checked either way, so this is reported as unknown rather than passing. A silent pass here would be a lie.",
      "fix": "Run the tool's version command by hand and open an issue with the output, so the parser can learn the shape.",
      "composed": true
    },
    {
      "rule_id": "lockfiles/format-supported",
      "prefix": "lockfiles",
      "suffix": "format-supported",
      "title": "The installed manager can use this lockfile",
      "means": "The package manager on PATH is at or above the floor this lockfile format requires.",
      "matters": "Recorded as a PASS because the absence of a finding and a verified match are different states, and a report that only lists problems cannot tell you which of the two it means.",
      "fix": "Nothing to do.",
      "composed": false
    },
    {
      "rule_id": "lockfiles/format-unknown",
      "prefix": "lockfiles",
      "suffix": "format-unknown",
      "title": "The manager is installed but did not report a version",
      "means": "The package manager resolves on PATH, but asking it for its version produced nothing, so whether it can read the lockfile is unknown.",
      "matters": "Reported as UNKNOWN rather than folded into 'not installed', because the two have different fixes and conflating them sends you looking for a missing program that is right there. A common cause on Windows is a wrapper script resolving ahead of the real executable.",
      "fix": "Run the manager's `--version` by hand. If that works, the resolution order on PATH is the problem rather than the installation; `devrepro path` shows what resolves first.",
      "composed": false
    },
    {
      "rule_id": "lockfiles/manager-missing",
      "prefix": "lockfiles",
      "suffix": "manager-missing",
      "title": "A lockfile with no package manager to read it",
      "means": "The repository contains a lockfile for a package manager that does not resolve on PATH.",
      "matters": "Nothing installs from it. This is a warning rather than a blocker because it is often correct: a project that moved from yarn to pnpm and left the old lockfile behind is untidy, not broken.",
      "fix": "Install the manager if the lockfile is current, or delete the lockfile if the project has moved on. Leaving both a `yarn.lock` and a `package-lock.json` in one directory means two machines can resolve two different dependency trees from the same commit.",
      "composed": false
    },
    {
      "rule_id": "lockfiles/runtime-mismatch",
      "prefix": "lockfiles",
      "suffix": "runtime-mismatch",
      "title": "The active runtime is outside the range this lockfile was solved for",
      "means": "The lockfile records the runtime range it resolved against -- `requires-python`, `engines.node`, `RUBY VERSION` -- and the runtime on PATH falls outside it.",
      "matters": "A dependency graph is solved for a specific runtime range. Installing it on a runtime outside that range gives you packages whose own constraints were never checked against what you are running, and the failure appears at import or build time rather than at install time.",
      "fix": "Switch to a runtime inside the recorded range for this project, or re-resolve the lockfile on the runtime you intend to use and commit the result. Do not install across the boundary and hope.",
      "composed": false
    },
    {
      "rule_id": "lockfiles/runtime-spec-unparseable",
      "prefix": "lockfiles",
      "suffix": "runtime-spec-unparseable",
      "title": "The lockfile pins a runtime range this tool cannot parse",
      "means": "The recorded range uses ecosystem shorthand -- npm's `^20`, pip's `~=3.11` -- that this project's version comparison does not implement.",
      "matters": "Reported rather than skipped so the gap is visible. A tool that silently ignores a constraint it cannot read is indistinguishable from one that checked and found no problem.",
      "fix": "Check the range by hand against the runtime you are using. Comparator syntax (`>=20`, `>=3.11,<3.13`) is understood and can be used in `.devrepro.toml` if you want this enforced.",
      "composed": false
    },
    {
      "rule_id": "lockfiles/tool-too-old",
      "prefix": "lockfiles",
      "suffix": "tool-too-old",
      "title": "The package manager is older than the lockfile format",
      "means": "This lockfile is written in a format version that the installed package manager predates.",
      "matters": "The manager does not usually refuse. npm 6 handed a `lockfileVersion: 3` file rewrites the entire tree in the old format, and cargo before 1.78 cannot read a `version = 4` `Cargo.lock` at all. The first outcome is worse than the second: the install appears to succeed and the damage arrives as an unreviewable diff in someone else's pull request.",
      "fix": "Upgrade the package manager to the version the format requires. Downgrading the lockfile instead means every other machine that already has the newer manager re-upgrades it, and the file thrashes.",
      "composed": false
    },
    {
      "rule_id": "lockfiles/unreadable",
      "prefix": "lockfiles",
      "suffix": "unreadable",
      "title": "The lockfile could not be parsed",
      "means": "The file exists but is not valid JSON, TOML or the text format its name implies -- usually a merge conflict left in place, or a truncated write.",
      "matters": "A lockfile that cannot be parsed cannot reproduce anything, and the error a package manager gives for one is rarely about the file.",
      "fix": "Regenerate it with its package manager rather than hand-editing. If the cause was a merge conflict, resolve the manifest first and then re-lock; resolving conflict markers inside a lockfile by hand produces a file that parses and is still wrong.",
      "composed": false
    },
    {
      "rule_id": "network/ca-bundle-missing",
      "prefix": "network",
      "suffix": "ca-bundle-missing",
      "title": "A CA bundle variable points at a file that is not there",
      "means": "`NODE_EXTRA_CA_CERTS`, `REQUESTS_CA_BUNDLE` or a sibling names a path that does not exist.",
      "matters": "The runtime falls back to its default trust store and the override does nothing, silently. Whoever set it believes the corporate CA is trusted, and it is not.",
      "fix": "Correct the path, or unset the variable so the fallback is deliberate rather than accidental.",
      "composed": false
    },
    {
      "rule_id": "network/checks-skipped",
      "prefix": "network",
      "suffix": "checks-skipped",
      "title": "Endpoint and TLS checks did not run, because they open connections",
      "means": "A scan reports proxy configuration -- read from environment variables, which needs no connection -- and stops there. Reachability and TLS checks require opening sockets to github.com, the npm registry and PyPI, and a scan does not do that unless asked.",
      "matters": "Reported rather than left silent, because a scan that says nothing about network health reads as a scan that found network health fine. Until this was fixed, these checks ran on every scan: three third-party hosts, and any proxy in the path, learned that this machine had run this tool.",
      "fix": "Run `devrepro doctor --allow-network`, or `devrepro network --allow-network` for the fuller DNS, TLS and registry diagnostics.",
      "composed": false
    },
    {
      "rule_id": "network/clock-skew",
      "prefix": "network",
      "suffix": "clock-skew",
      "title": "System clock is significantly wrong",
      "means": "The system clock differs from network time by more than the tolerated margin.",
      "matters": "TLS certificate validation fails when the clock is far enough off, which breaks package installs, git over HTTPS and container pulls with errors that blame the certificate rather than the clock.",
      "fix": "Enable automatic time synchronisation for your OS.",
      "composed": false
    },
    {
      "rule_id": "network/endpoint-unreachable",
      "prefix": "network",
      "suffix": "endpoint-unreachable",
      "title": "A development endpoint did not respond",
      "means": "One of the checked registries or hosts was unreachable, or its TLS chain did not validate.",
      "matters": "Package installs will fail. Behind a corporate proxy this is usually TLS interception rather than an outage.",
      "fix": "Check proxy settings and whether your organisation's CA is trusted by the language runtime you are using -- Node, Python, Go and Java each keep their own trust store.",
      "composed": false
    },
    {
      "rule_id": "network/endpoints-ok",
      "prefix": "network",
      "suffix": "endpoints-ok",
      "title": "Development endpoints reachable",
      "means": "The checked registries responded with a valid TLS chain.",
      "matters": "Reported so a working network path is visible in the report.",
      "fix": "No action needed.",
      "composed": false
    },
    {
      "rule_id": "network/registry-override",
      "prefix": "network",
      "suffix": "registry-override",
      "title": "Packages come from somewhere other than the public registry",
      "means": "An `.npmrc`, `pip.conf`, `.cargo/config.toml` or similar points at a mirror or proxy.",
      "matters": "Normal on its own -- mirrors exist for good reasons. It matters when one machine has the override and another does not: the same install command then fetches different bytes, the lockfile still matches, and nothing reports a difference. A user-scoped override is the harder case, because it is invisible to everyone else on the team.",
      "fix": "Confirm the whole team shares the configuration, and prefer a project-scoped file over a user-scoped one so it is reviewable.",
      "composed": false
    },
    {
      "rule_id": "network/trust-store-partial",
      "prefix": "network",
      "suffix": "trust-store-partial",
      "title": "Some runtimes trust the corporate CA and others do not",
      "means": "A custom CA bundle is configured for at least one ecosystem and not for others.",
      "matters": "Every runtime keeps its own trust store: Node reads `NODE_EXTRA_CA_CERTS`, Python `REQUESTS_CA_BUNDLE`, Go `SSL_CERT_FILE`, git `GIT_SSL_CAINFO`. Behind a TLS-intercepting proxy, configuring one means `npm install` works and `pip install` fails on the same machine, with an error that blames the certificate rather than the missing variable.",
      "fix": "Set the corresponding variable for each ecosystem you use. There is no single setting that covers them all, which is the whole difficulty.",
      "composed": false
    },
    {
      "rule_id": "node/known-bad-version",
      "prefix": "node",
      "suffix": "known-bad-version",
      "title": "Version explicitly forbidden by policy",
      "means": "The installed version matches a range listed under `[known_bad_versions]` in the project's `.devrepro.toml`.",
      "matters": "Someone recorded that this exact version breaks the project. That is more specific than a range check and usually earned the hard way.",
      "fix": "Move off the forbidden version. The policy file should say why; if it does not, add the reason for whoever hits it next.",
      "composed": true
    },
    {
      "rule_id": "node/manager-conflict",
      "prefix": "node",
      "suffix": "manager-conflict",
      "title": "Two version managers fighting over one ecosystem",
      "means": "More than one version manager for this ecosystem initialises in your shell profiles -- for example both nvm and fnm, or both conda and pyenv.",
      "matters": "The last one to run wins, and which one that is depends on the order of lines in files you rarely read. The result is a tool version that changes when you open a different kind of terminal.",
      "fix": "Pick one manager per ecosystem and remove the other's init lines from your shell profile. `devrepro envmanagers` shows what is declared and what is active.",
      "composed": true
    },
    {
      "rule_id": "node/missing",
      "prefix": "node",
      "suffix": "missing",
      "title": "Required tool not found",
      "means": "The project declares this tool as a requirement and it does not resolve on PATH.",
      "matters": "Any command that needs it fails immediately, usually with a confusing 'not recognized' or 'command not found' rather than a note about the declared requirement.",
      "fix": "Install it, or remove the requirement from the manifest if it is no longer real. Check `devrepro which <tool>` first -- the tool may be installed but shadowed or off PATH in this shell.",
      "composed": true
    },
    {
      "rule_id": "node/multiple-installations",
      "prefix": "node",
      "suffix": "multiple-installations",
      "title": "Several copies of the same tool on PATH",
      "means": "More than one executable with this name resolves on PATH. The first wins, and the others are reachable only by full path.",
      "matters": "The one that wins depends on PATH order, which differs between your shell, your editor's terminal, and CI. That is how the same command produces different versions in different windows on one machine.",
      "fix": "Run `devrepro which <tool>` to see every candidate and which one wins. Keep the installation you intend to use and remove or de-prefer the rest; `devrepro plan` proposes the PATH edit.",
      "composed": true
    },
    {
      "rule_id": "node/shim-bypassed",
      "prefix": "node",
      "suffix": "shim-bypassed",
      "title": "A version manager is installed but not being used",
      "means": "The manager's shim directory is on PATH, but another installation of this tool resolves before it.",
      "matters": "The manager still reports the version it intends -- `pyenv version` says 3.12 -- while every command gets something else, because resolution is decided by PATH order and nothing else. Neither tool is wrong about what it was asked, so nothing reports a problem, and the project's pinned version is quietly not in effect.",
      "fix": "Move the manager's shim directory earlier in PATH than the installation that currently wins. `devrepro which <tool>` lists every candidate in precedence order. Note that an IDE's integrated terminal often has a different PATH from a login shell, so check the one you actually build in.",
      "composed": true
    },
    {
      "rule_id": "node/version-mismatch",
      "prefix": "node",
      "suffix": "version-mismatch",
      "title": "Version outside the declared range",
      "means": "The tool is installed, but its version does not satisfy what the project's manifest requires.",
      "matters": "This is the most common cause of a build that works for one person and not another. Lockfiles, syntax features and native modules are all version-sensitive, and the failure usually surfaces far from the cause.",
      "fix": "Install a version inside the declared range, or widen the range if the declaration is stale. A version manager (mise, asdf, nvm, pyenv) makes this per-project rather than global.",
      "composed": true
    },
    {
      "rule_id": "node/version-ok",
      "prefix": "node",
      "suffix": "version-ok",
      "title": "Version satisfies the requirement",
      "means": "The installed version falls inside the range the project declares.",
      "matters": "Nothing to do. It is reported so a passing check is visible rather than inferred from silence.",
      "fix": "No action needed.",
      "composed": true
    },
    {
      "rule_id": "node/version-unparseable",
      "prefix": "node",
      "suffix": "version-unparseable",
      "title": "Version string could not be read",
      "means": "The tool responded, but its version output did not match any pattern this project knows how to parse.",
      "matters": "The requirement cannot be checked either way, so this is reported as unknown rather than passing. A silent pass here would be a lie.",
      "fix": "Run the tool's version command by hand and open an issue with the output, so the parser can learn the shape.",
      "composed": true
    },
    {
      "rule_id": "npm/multiple-installations",
      "prefix": "npm",
      "suffix": "multiple-installations",
      "title": "Several copies of the same tool on PATH",
      "means": "More than one executable with this name resolves on PATH. The first wins, and the others are reachable only by full path.",
      "matters": "The one that wins depends on PATH order, which differs between your shell, your editor's terminal, and CI. That is how the same command produces different versions in different windows on one machine.",
      "fix": "Run `devrepro which <tool>` to see every candidate and which one wins. Keep the installation you intend to use and remove or de-prefer the rest; `devrepro plan` proposes the PATH edit.",
      "composed": true
    },
    {
      "rule_id": "openssl/known-advisory",
      "prefix": "openssl",
      "suffix": "known-advisory",
      "title": "The installed tool version is covered by a published advisory",
      "means": "The version of this build tool matches an entry in the offline advisory set that ships with devrepro, or in the bundle you supplied with `--db`.",
      "matters": "Dependency scanners never look at the compiler, the interpreter or git, because none of them appear in a lockfile. A toolchain can therefore sit years out of date behind a perfectly clean audit.",
      "fix": "Upgrade the tool to the fix on its own release branch -- backported fixes mean the highest version number is not always the answer. This comparison is offline and does not know whether your distribution backported the fix into the version string you have, which many do; check your distribution's changelog before treating it as urgent.",
      "composed": false
    },
    {
      "rule_id": "path/dead-entries",
      "prefix": "path",
      "suffix": "dead-entries",
      "title": "PATH entries that do not exist",
      "means": "PATH lists directories that are not present on disk.",
      "matters": "The OS still stats every one of them on every lookup. On Windows in particular this measurably slows down each new process.",
      "fix": "Remove the entries. If one was supposed to exist, the tool that owns it probably failed to install.",
      "composed": false
    },
    {
      "rule_id": "path/duplicates",
      "prefix": "path",
      "suffix": "duplicates",
      "title": "Duplicate PATH entries",
      "means": "The same directory appears more than once in PATH.",
      "matters": "Every duplicate is searched again on every command lookup, so process startup gets slower for no benefit. It is also a sign that a profile is appending to PATH each time it is sourced.",
      "fix": "Remove the later occurrences, keeping the first. This never changes which executable wins, so it is a SAFE remediation.",
      "composed": false
    },
    {
      "rule_id": "path/store-aliases",
      "prefix": "path",
      "suffix": "store-aliases",
      "title": "Windows Store execution aliases on PATH",
      "means": "The `WindowsApps` alias directory is on PATH. It contains stub executables that open the Microsoft Store instead of running a tool.",
      "matters": "The stubs commonly shadow real installations -- the classic symptom is `python` opening the Store rather than the Python you installed.",
      "fix": "Settings > Apps > Advanced app settings > App execution aliases, and turn off the aliases for tools you have installed properly.",
      "composed": false
    },
    {
      "rule_id": "pip/multiple-installations",
      "prefix": "pip",
      "suffix": "multiple-installations",
      "title": "Several copies of the same tool on PATH",
      "means": "More than one executable with this name resolves on PATH. The first wins, and the others are reachable only by full path.",
      "matters": "The one that wins depends on PATH order, which differs between your shell, your editor's terminal, and CI. That is how the same command produces different versions in different windows on one machine.",
      "fix": "Run `devrepro which <tool>` to see every candidate and which one wins. Keep the installation you intend to use and remove or de-prefer the rest; `devrepro plan` proposes the PATH edit.",
      "composed": true
    },
    {
      "rule_id": "ports/port-in-use",
      "prefix": "ports",
      "suffix": "port-in-use",
      "title": "A declared port is already bound",
      "means": "A port the project declares is in use by another process.",
      "matters": "The service will fail to start, usually with an 'address already in use' that does not say what is holding it.",
      "fix": "Stop the process holding the port, or change the port the project declares.",
      "composed": false
    },
    {
      "rule_id": "python/known-advisory",
      "prefix": "python",
      "suffix": "known-advisory",
      "title": "The installed tool version is covered by a published advisory",
      "means": "The version of this build tool matches an entry in the offline advisory set that ships with devrepro, or in the bundle you supplied with `--db`.",
      "matters": "Dependency scanners never look at the compiler, the interpreter or git, because none of them appear in a lockfile. A toolchain can therefore sit years out of date behind a perfectly clean audit.",
      "fix": "Upgrade the tool to the fix on its own release branch -- backported fixes mean the highest version number is not always the answer. This comparison is offline and does not know whether your distribution backported the fix into the version string you have, which many do; check your distribution's changelog before treating it as urgent.",
      "composed": false
    },
    {
      "rule_id": "python/known-bad-version",
      "prefix": "python",
      "suffix": "known-bad-version",
      "title": "Version explicitly forbidden by policy",
      "means": "The installed version matches a range listed under `[known_bad_versions]` in the project's `.devrepro.toml`.",
      "matters": "Someone recorded that this exact version breaks the project. That is more specific than a range check and usually earned the hard way.",
      "fix": "Move off the forbidden version. The policy file should say why; if it does not, add the reason for whoever hits it next.",
      "composed": true
    },
    {
      "rule_id": "python/manager-conflict",
      "prefix": "python",
      "suffix": "manager-conflict",
      "title": "Two version managers fighting over one ecosystem",
      "means": "More than one version manager for this ecosystem initialises in your shell profiles -- for example both nvm and fnm, or both conda and pyenv.",
      "matters": "The last one to run wins, and which one that is depends on the order of lines in files you rarely read. The result is a tool version that changes when you open a different kind of terminal.",
      "fix": "Pick one manager per ecosystem and remove the other's init lines from your shell profile. `devrepro envmanagers` shows what is declared and what is active.",
      "composed": true
    },
    {
      "rule_id": "python/missing",
      "prefix": "python",
      "suffix": "missing",
      "title": "Required tool not found",
      "means": "The project declares this tool as a requirement and it does not resolve on PATH.",
      "matters": "Any command that needs it fails immediately, usually with a confusing 'not recognized' or 'command not found' rather than a note about the declared requirement.",
      "fix": "Install it, or remove the requirement from the manifest if it is no longer real. Check `devrepro which <tool>` first -- the tool may be installed but shadowed or off PATH in this shell.",
      "composed": true
    },
    {
      "rule_id": "python/multiple-installations",
      "prefix": "python",
      "suffix": "multiple-installations",
      "title": "Several copies of the same tool on PATH",
      "means": "More than one executable with this name resolves on PATH. The first wins, and the others are reachable only by full path.",
      "matters": "The one that wins depends on PATH order, which differs between your shell, your editor's terminal, and CI. That is how the same command produces different versions in different windows on one machine.",
      "fix": "Run `devrepro which <tool>` to see every candidate and which one wins. Keep the installation you intend to use and remove or de-prefer the rest; `devrepro plan` proposes the PATH edit.",
      "composed": true
    },
    {
      "rule_id": "python/multiple-versions",
      "prefix": "python",
      "suffix": "multiple-versions",
      "title": "Several Python versions installed",
      "means": "More than one Python interpreter is present with differing versions.",
      "matters": "Not a problem in itself -- it is normal and often deliberate. It is reported because it is the context you need when a package installs into an interpreter other than the one you are running.",
      "fix": "No action needed unless a requirement is unmet. Use a per-project version manager or a virtualenv to make the choice explicit.",
      "composed": false
    },
    {
      "rule_id": "python/shim-bypassed",
      "prefix": "python",
      "suffix": "shim-bypassed",
      "title": "A version manager is installed but not being used",
      "means": "The manager's shim directory is on PATH, but another installation of this tool resolves before it.",
      "matters": "The manager still reports the version it intends -- `pyenv version` says 3.12 -- while every command gets something else, because resolution is decided by PATH order and nothing else. Neither tool is wrong about what it was asked, so nothing reports a problem, and the project's pinned version is quietly not in effect.",
      "fix": "Move the manager's shim directory earlier in PATH than the installation that currently wins. `devrepro which <tool>` lists every candidate in precedence order. Note that an IDE's integrated terminal often has a different PATH from a login shell, so check the one you actually build in.",
      "composed": true
    },
    {
      "rule_id": "python/store-alias-shadow",
      "prefix": "python",
      "suffix": "store-alias-shadow",
      "title": "A Store alias is shadowing a real Python",
      "means": "A `WindowsApps` python stub resolves before a genuine Python installation.",
      "matters": "`python` will not run the interpreter you installed. Virtualenv creation, pip and every script fail in ways that do not mention the alias.",
      "fix": "Disable the python app execution alias, or move the real installation's directory earlier in PATH.",
      "composed": false
    },
    {
      "rule_id": "python/version-mismatch",
      "prefix": "python",
      "suffix": "version-mismatch",
      "title": "Version outside the declared range",
      "means": "The tool is installed, but its version does not satisfy what the project's manifest requires.",
      "matters": "This is the most common cause of a build that works for one person and not another. Lockfiles, syntax features and native modules are all version-sensitive, and the failure usually surfaces far from the cause.",
      "fix": "Install a version inside the declared range, or widen the range if the declaration is stale. A version manager (mise, asdf, nvm, pyenv) makes this per-project rather than global.",
      "composed": true
    },
    {
      "rule_id": "python/version-ok",
      "prefix": "python",
      "suffix": "version-ok",
      "title": "Version satisfies the requirement",
      "means": "The installed version falls inside the range the project declares.",
      "matters": "Nothing to do. It is reported so a passing check is visible rather than inferred from silence.",
      "fix": "No action needed.",
      "composed": true
    },
    {
      "rule_id": "python/version-unparseable",
      "prefix": "python",
      "suffix": "version-unparseable",
      "title": "Version string could not be read",
      "means": "The tool responded, but its version output did not match any pattern this project knows how to parse.",
      "matters": "The requirement cannot be checked either way, so this is reported as unknown rather than passing. A silent pass here would be a lie.",
      "fix": "Run the tool's version command by hand and open an issue with the output, so the parser can learn the shape.",
      "composed": true
    },
    {
      "rule_id": "ruby/shim-bypassed",
      "prefix": "ruby",
      "suffix": "shim-bypassed",
      "title": "A version manager is installed but not being used",
      "means": "The manager's shim directory is on PATH, but another installation of this tool resolves before it.",
      "matters": "The manager still reports the version it intends -- `pyenv version` says 3.12 -- while every command gets something else, because resolution is decided by PATH order and nothing else. Neither tool is wrong about what it was asked, so nothing reports a problem, and the project's pinned version is quietly not in effect.",
      "fix": "Move the manager's shim directory earlier in PATH than the installation that currently wins. `devrepro which <tool>` lists every candidate in precedence order. Note that an IDE's integrated terminal often has a different PATH from a login shell, so check the one you actually build in.",
      "composed": true
    },
    {
      "rule_id": "rust/known-bad-version",
      "prefix": "rust",
      "suffix": "known-bad-version",
      "title": "Version explicitly forbidden by policy",
      "means": "The installed version matches a range listed under `[known_bad_versions]` in the project's `.devrepro.toml`.",
      "matters": "Someone recorded that this exact version breaks the project. That is more specific than a range check and usually earned the hard way.",
      "fix": "Move off the forbidden version. The policy file should say why; if it does not, add the reason for whoever hits it next.",
      "composed": true
    },
    {
      "rule_id": "rust/missing",
      "prefix": "rust",
      "suffix": "missing",
      "title": "Required tool not found",
      "means": "The project declares this tool as a requirement and it does not resolve on PATH.",
      "matters": "Any command that needs it fails immediately, usually with a confusing 'not recognized' or 'command not found' rather than a note about the declared requirement.",
      "fix": "Install it, or remove the requirement from the manifest if it is no longer real. Check `devrepro which <tool>` first -- the tool may be installed but shadowed or off PATH in this shell.",
      "composed": true
    },
    {
      "rule_id": "rust/version-mismatch",
      "prefix": "rust",
      "suffix": "version-mismatch",
      "title": "Version outside the declared range",
      "means": "The tool is installed, but its version does not satisfy what the project's manifest requires.",
      "matters": "This is the most common cause of a build that works for one person and not another. Lockfiles, syntax features and native modules are all version-sensitive, and the failure usually surfaces far from the cause.",
      "fix": "Install a version inside the declared range, or widen the range if the declaration is stale. A version manager (mise, asdf, nvm, pyenv) makes this per-project rather than global.",
      "composed": true
    },
    {
      "rule_id": "rust/version-ok",
      "prefix": "rust",
      "suffix": "version-ok",
      "title": "Version satisfies the requirement",
      "means": "The installed version falls inside the range the project declares.",
      "matters": "Nothing to do. It is reported so a passing check is visible rather than inferred from silence.",
      "fix": "No action needed.",
      "composed": true
    },
    {
      "rule_id": "rust/version-unparseable",
      "prefix": "rust",
      "suffix": "version-unparseable",
      "title": "Version string could not be read",
      "means": "The tool responded, but its version output did not match any pattern this project knows how to parse.",
      "matters": "The requirement cannot be checked either way, so this is reported as unknown rather than passing. A silent pass here would be a lie.",
      "fix": "Run the tool's version command by hand and open an issue with the output, so the parser can learn the shape.",
      "composed": true
    },
    {
      "rule_id": "shell/managers-initialized",
      "prefix": "shell",
      "suffix": "managers-initialized",
      "title": "Version managers initialised by shell profiles",
      "means": "Which tool managers your shell profiles set up at startup.",
      "matters": "Context for PATH ordering and for `manager-conflict`.",
      "fix": "No action needed.",
      "composed": false
    },
    {
      "rule_id": "system/os-detected",
      "prefix": "system",
      "suffix": "os-detected",
      "title": "Operating system detected",
      "means": "The OS, version and architecture this scan ran on.",
      "matters": "Every other finding is relative to this. It is also the first thing anyone comparing two machines wants.",
      "fix": "No action needed.",
      "composed": false
    },
    {
      "rule_id": "system/resources",
      "prefix": "system",
      "suffix": "resources",
      "title": "CPU, memory and disk headroom",
      "means": "Totals only -- no serial numbers, no SMART data.",
      "matters": "Builds fail in confusing ways when a disk is nearly full, and a container build can exhaust one quietly.",
      "fix": "Free space if the report shows little headroom.",
      "composed": false
    },
    {
      "rule_id": "system/shell",
      "prefix": "system",
      "suffix": "shell",
      "title": "Active shell",
      "means": "Which shell this scan believes you are running.",
      "matters": "Determines which profile files matter for PATH and manager initialisation.",
      "fix": "No action needed.",
      "composed": false
    },
    {
      "rule_id": "virt/status",
      "prefix": "virt",
      "suffix": "status",
      "title": "Virtualization capability",
      "means": "Whether hardware virtualization and the relevant hypervisor are available.",
      "matters": "WSL2 and container backends need it. It is often disabled in firmware on new machines.",
      "fix": "Enable virtualization in firmware if a container backend needs it.",
      "composed": false
    },
    {
      "rule_id": "wsl/detected",
      "prefix": "wsl",
      "suffix": "detected",
      "title": "WSL is available",
      "means": "WSL was found, with its distributions listed.",
      "matters": "Recorded because a Windows machine with WSL behaves very differently from one without, and the default distro determines which filesystem commands run against.",
      "fix": "No action needed.",
      "composed": false
    },
    {
      "rule_id": "wsl/interop-disabled",
      "prefix": "wsl",
      "suffix": "interop-disabled",
      "title": "WSL interop is disabled",
      "means": "Windows/Linux interop is turned off in this WSL installation.",
      "matters": "Calling Windows executables from inside WSL fails, which breaks many cross-platform toolchains.",
      "fix": "Enable `interop` in `/etc/wsl.conf` and restart WSL.",
      "composed": false
    },
    {
      "rule_id": "wsl/no-default-distro",
      "prefix": "wsl",
      "suffix": "no-default-distro",
      "title": "WSL has no default distribution",
      "means": "WSL is installed but no distribution is set as default.",
      "matters": "`wsl <command>` fails, and tools that shell into WSL fail with it.",
      "fix": "`wsl --set-default <distro>`.",
      "composed": false
    },
    {
      "rule_id": "wsl/not-installed",
      "prefix": "wsl",
      "suffix": "not-installed",
      "title": "WSL is not installed",
      "means": "No WSL installation was detected on this Windows machine.",
      "matters": "Matters only if the project expects a Linux environment.",
      "fix": "`wsl --install` if the project needs it.",
      "composed": false
    }
  ]
}
